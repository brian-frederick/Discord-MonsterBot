const modifier = {
  name: '- Modifier (number)',
  value: 'A positive or negative number that impacts the outcome.'
};

const tag = {
  name: '- Tag (mention)',
  value: `@ another hunter to use their properties instead of the sender's.`
}

module.exports = { modifier, tag };
